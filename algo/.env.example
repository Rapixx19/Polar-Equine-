ALGO_BEARER_TOKEN=
# Slice 10: required for /compute + /recompute to read samples and write metrics.
# Use the SAME values Vercel knows; SUPABASE_URL is your project URL,
# SUPABASE_SERVICE_ROLE_KEY is the service-role secret from Supabase dashboard.
# See docs/shared/01-environment-variables.md for the canonical list.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
